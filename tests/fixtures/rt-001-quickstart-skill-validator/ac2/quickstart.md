# AC-2 fixture

Run `bmad-loop run` to start automation, or `bmad-loop tui` for the
dashboard. The `gds` module isn't covered by this fixture.

References: `bmad-prd`, `wds-1-project-brief`, and `bmad-prd` again.
