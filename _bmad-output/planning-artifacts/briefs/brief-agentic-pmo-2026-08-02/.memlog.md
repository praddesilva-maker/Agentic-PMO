---
topic: Agentic PMO platform: skills layer + tools layer, Jira/Confluence connector first, MS surface later
updated: 2026-08-02T17:03
---

- (note by user) core idea: a portable Atlassian connector using Atlassian's REST APIs
- (note by user) use case 1: as a Delivery Lead, use Claude to read and write Jira and Confluence
- (note by user) use case 2: as a Product Manager, want the connector abstracted and available to any delivery lead who wants it
- (event) session started via bmad-product-brief skill, Create intent
- (insight by research) Atlassian shipped an official Remote MCP Server (GA Feb 2026, mcp.atlassian.com) covering Jira/Confluence/JSM/Bitbucket/Compass with 72+ tools, OAuth 2.1 per-user + admin-gated shared-token mode for automation -- directly relevant to buy-vs-build
- (note by research) community alternative sooperset/mcp-atlassian exists but multi-user/concurrent HTTP auth story still maturing (open issues #380/#610/#850)
- (note by research) auth model verdict for shared multi-user tool: OAuth 2.0/2.1 3LO per-user is the standard fit; API tokens are single-identity, Connect is sunsetting, Forge is heavier (hosted app model)
- (note by research) API gotchas: Jira /search endpoint retired Oct 2025 (now /search/jql, token pagination, no total count); Confluence v2 API missing whiteboards/databases/folders vs v1; comment/description bodies are ADF JSON not plain text; JQL needs accountId not username
- (note by research) rate limits: points-based system for OAuth apps from March 2026, burst limits on raw API tokens since Nov 2025, per-tenant so concurrent delivery leads on same Atlassian site share quota
- (event) background research digest received and logged
- (decision by user) PIVOT: real intent is not just an Atlassian connector -- it's an 'Agentic PMO' app that assists PMs/Delivery Leads/others to initiate, plan, and deliver projects with agent assistance (e.g. Claude)
- (idea by user) product shape: a skills layer (user-facing agentic workflows/UI) plus a tools layer (reusable connectors -- Jira/Confluence now, MS Teams/SharePoint/OneDrive future) so agents don't rebuild connectors every session and burn credits
- (gap by user) driver: Rovo MCP about to be blocked from public-internet access by user's org
- (gap by user) driver: Atlassian likely to start charging for Rovo
- (question) is this repo (Agentic PMO) intended as the home/seed for this actual product, given the name match and still-empty CLAUDE.md project context
- (decision by user) current repo (Agentic-PMO) will be renamed to 'BMAD Team' -- it's the BMAD tooling/planning workspace, not the product; a NEW repo will be created to house the actual Agentic PMO product
- (decision by user) brief scope: cover the whole platform vision (skills layer + tools layer), not just the connector piece
- (note by user) the 'agents rebuild connectors every session, burn credits' problem has been directly observed, in a separate repo not yet shared
- (gap by user) major driver: company is building an internal MCP Gateway and will require individual company-specific MCPs with PII protections in place; timeline is 'next few weeks'
- (note by user) prior related material exists (a 'vibe coded' repo) but user doesn't have access right now, will share later
- (event) user confirmed: draft fast-path, full brief with ASSUMPTION tags
- (event) first full draft written: brief.md + addendum.md
- (event) polish pass complete: structural + prose review run on brief.md and addendum.md (4 parallel subagent passes), accepted fixes applied -- Open Items section added consolidating all ASSUMPTION flags, redundant Rovo/Gateway restatements trimmed, addendum reorganized into real subheadings, dates/acronyms standardized
- (event) brief finalized: no external_handoffs configured for this skill, artifacts remain local
- (gap by user) technical constraint: Atlassian API work requires Node 22.22 specifically -- reason not yet confirmed, research in progress
- (event) moving to PRD stage via bmad-prd
- (note by user) user shared prior 'vibe coded' repo: /home/praddesilva/ProjectTeams/SRG-Apps/Apps/Copilot-PMAgentv2 -- real MCP server (@modelcontextprotocol/sdk), does Jira/Confluence read+update+CREATE (not just read/update)
- (insight by research) audit findings: 22 real MCP tools, narrow create-only/update-only write-tool philosophy (D-002/D-003), ADF validator before writes, optimistic locking, idempotent append blocks -- strong safety patterns worth reusing conceptually
- (gap by research) auth model gap: single shared Basic-Auth API token per server process, 'per-user' achieved by each person running own local instance -- does NOT satisfy true multi-user shared-service per-user-identity requirement. This is the one piece that can't carry forward as-is.
- (insight by research) Node 22.22 RESOLVED: extensively documented in Copilot-PMAgentv2 as team convention (CLAUDE.md, hardcoded path C:\Apps\tools\node22.22 in .vscode/mcp.json), NOT an engines/nvmrc/CI pin, NOT an Atlassian requirement -- informal convention specific to that prior project
- (gap by research) D-001 decision in prior repo: a centrally-hosted Azure MCP server was considered and REJECTED due to lack of Azure subscription/Power Platform admin rights -- real precedent risk for the new company MCP Gateway approach
- (question by research) unresolved: stray untracked files suggest a possibly more advanced parallel TypeScript rewrite exists ('PM-Agent-Service' / 'Transformation-PM-Agent-Service' repo, Vitest tests, mcp-adapter + engines + Program Registry pattern) -- could not verify from within Copilot-PMAgentv2 checkout, need to ask user directly
- (decision by user) mid-session: reframe brief+PRD based on these findings while user is away ~1hr; user pre-authorized reasonable assumptions
- (event) brief.md + addendum.md reframed with Copilot-PMAgentv2 audit findings: creation moved into scope, auth-model gap flagged, MCP Gateway/Azure precedent risk added, Node 22.22 resolved
